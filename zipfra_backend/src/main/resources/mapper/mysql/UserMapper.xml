<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.zipfra.mapper.mysql.UserMapper">

    <resultMap id="UserResultMap" type="com.example.zipfra.domain.User">
        <id property="id" column="id"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="profileImageUrl" column="profile_image_url"/>
        <result property="role" column="role"/>
        <result property="isActive" column="is_active"/>
    </resultMap>

    <select id="findByEmail" resultMap="UserResultMap">
        SELECT id, email, password, nickname, profile_image_url, role, is_active
        FROM users
        WHERE email = #{email} AND is_active = 1
    </select>

    <select id="findById" resultMap="UserResultMap">
        SELECT id, email, password, nickname, profile_image_url, role, is_active
        FROM users
        WHERE id = #{id} AND is_active = 1
    </select>

    <insert id="insertUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (email, password, nickname, profile_image_url, role, is_active)
        VALUES (#{email}, #{password}, #{nickname}, #{profileImageUrl}, #{role}, #{isActive})
    </insert>

    <update id="updateUser">
        UPDATE users
        SET nickname = #{nickname}, password = #{password}, profile_image_url = #{profileImageUrl}
        WHERE id = #{id} AND is_active = 1
    </update>

    <delete id="deactivateUser">
        DELETE FROM users
        WHERE id = #{id}
    </delete>

    <update id="updatePasswordByEmail">
        UPDATE users
        SET password = #{password}
        WHERE email = #{email} AND is_active = 1
    </update>

    <select id="searchByNickname" resultMap="UserResultMap">
        SELECT id, email, password, nickname, profile_image_url, role, is_active
        FROM users
        WHERE nickname LIKE CONCAT('%', #{nickname}, '%') AND is_active = 1
    </select>

</mapper>
